const InjectToken = Object.freeze({
   // services
   RegisterService: "RegisterService",

   // repository
   UserRepository: "UserRepository",

   // helper
   JwtHelper: "JwtHelper"
})

export {InjectToken}